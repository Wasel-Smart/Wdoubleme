# Wasel App — Cleanup Report
Generated by deep codebase analysis · Claude

---

## What CLEANUP.bat Does

All files are **moved to `_REMOVED\`** before deletion — nothing is permanently
lost until you manually delete that folder. Safe and fully reversible.

---

## Files & Folders Being Removed

### Phase 1 — `/src/` Root Junk (10 files)
| File | Reason |
|------|--------|
| `FINAL_DELIVERY_SUMMARY.md` | Dev summary doc, not app code |
| `IMPLEMENTATION_SUMMARY.md` | Dev summary doc, not app code |
| `MOBILITY_OS_IMPLEMENTATION.md` | Dev summary doc, not app code |
| `QUICK_START_NEW_FEATURES.md` | Dev summary doc, not app code |
| `Attributions.md` | Attribution note, not app code |
| `fix-build.bat` / `fix-build.sh` | One-time build fix scripts |
| `test-backend-health.sh` | One-time test script |
| `test-wasel-ds-exports.ts` | One-time export test |
| `GoogleService-Info.plist` | iOS Firebase config — web-only project |
| `lighthouserc.json` | Lighthouse CI config — not wired to build |
| `drizzle.config.ts` | Drizzle ORM config — app uses Supabase, not Drizzle |

### Phase 2 — `/src/docs/` Fix Reports (3 files)
| File | Reason |
|------|--------|
| `DOWNLOAD_ERROR_FIX_REPORT.md` | Post-fix documentation |
| `DOWNLOAD_FIX_VALIDATION.md` | Post-fix validation notes |
| `QUICK_FIX_SUMMARY.md` | One-time fix summary |

### Phase 3 — `/src/scripts/` Fix Scripts (3 files)
| File | Reason |
|------|--------|
| `verify-download-fix.bat/.sh` | One-time verification scripts |
| `fix-api-paths.sh` | One-time path-fix script |

### Phase 4 — `/src/imports/` ENTIRE FOLDER (~350 KB)
Planning documents, pasted text files, strategy notes, prototypes.
None of these are imported by any component. Includes:
- `pasted_text/` subfolder with 22 files
- Market analysis, roadmaps, architecture plans
- A 105 KB duplicate of an old landing page
- `google-services.json` (wrong location — belongs in project root if needed)

### Phase 5 — `/src/gxp/` ENTIRE FOLDER (52 KB)
GxP pharmaceutical compliance system. Not referenced anywhere in
the router or components. Not relevant to a ride/transport platform.

### Phase 6 — Empty Directories
- `/src/routes/` — empty, router logic lives in `wasel-routes.tsx`

### Phase 7 — Unused Components (router-confirmed)
| File | Reason |
|------|--------|
| `AuthFix.tsx` | Auth patch — `WaselAuth.tsx` is the canonical auth page |
| `LandingPage.tsx` (108 KB!) | Old landing — router uses `WaselLanding.tsx` |
| `LandingPageWrapper.tsx` | Wrapper for the deleted LandingPage |
| `TripsAnalyticsDashboard.tsx` | 282-byte stub with no content |
| `GoogleMapComponent.tsx` | 759-byte stub — `WaselMap.tsx` is the real map |
| `MapComponent.tsx` | 620-byte stub — same reason |
| `WorkflowGuide.tsx` | Internal dev guide component, not routed |
| `APIKeysSetupGuide.tsx` | Dev setup guide, not user-facing |

### Phase 8 — Figma Dev Tooling (not app logic)
| File | Reason |
|------|--------|
| `figmaMessagePortHandler.ts` | Figma plugin bridge — not app code |
| `figmaMessagePortHandler.test.ts` | Test for above |
| `uatTestData.ts` | UAT seed data — not production code |

### Phase 9 — Duplicate Config
| File | Reason |
|------|--------|
| `vitest.config.mjs` | Duplicate of `vitest.config.ts` — `.ts` is kept |

### Phase 10 — Misplaced CI/CD Files (moved, not deleted)
`src/workflows/*.yml` → `.github/workflows/`
These are GitHub Actions files and must live in `.github/workflows/`
to be picked up by GitHub CI. They were in the wrong location.

---

## Space Recovered
| Category | Approx Size |
|----------|------------|
| `/src/imports/` folder | ~350 KB |
| `/src/gxp/` folder | ~52 KB |
| Junk docs & scripts | ~80 KB |
| Unused components | ~155 KB |
| Figma/UAT utils | ~30 KB |
| **Total** | **~667 KB** |

---

## What Was NOT Touched

The following were flagged but left alone — they need deeper review:
- `AuthPage.tsx` — may be used internally by `WaselAuth.tsx`
- `AuthModal.tsx` — may be used by `AuthPage.tsx`
- `analytics.ts` + `analyticsService.ts` — need merge analysis
- `monitoring.tsx` + `monitoring.ts` — need merge analysis
- `src/features/testing/` — huge QA dashboards; disable via feature flag instead
- `src/cypress/` — keep if you're using Cypress; delete if Playwright-only
- `build/` folder — safe to delete anytime (regenerated by `npm run build`)

---

## Next Steps After Running Cleanup

1. Run `npm run build` to confirm nothing breaks
2. Run `npm run dev` and test core flows
3. If all good, delete the `_REMOVED\` backup folder
4. Consider merging `analytics.ts` + `analyticsService.ts`
5. Consider merging `monitoring.ts` + `monitoring.tsx`
